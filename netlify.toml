# Netlify configuration for Angular 20 SSR
[build]
  publish = "dist/local-walls/browser"
  command = "npm run build"

# Redirección de SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
