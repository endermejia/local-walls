/**
 * Example environment file for local setup.
 *
 * Do NOT commit real credentials. Copy this file to `src/environments/environment.ts`
 * and fill in your actual Supabase credentials locally.
 *
 * Windows PowerShell example:
 *   Copy-Item src/environments/environment.example.ts src/environments/environment.ts
 *
 * macOS/Linux (if applicable):
 *   cp src/environments/environment.example.ts src/environments/environment.ts
 */

// Supabase project URL (e.g., https://xyzcompany.supabase.co)
export const ENV_SUPABASE_URL = '';

// Supabase anonymous key (public anon key)
export const ENV_SUPABASE_ANON_KEY = '';
